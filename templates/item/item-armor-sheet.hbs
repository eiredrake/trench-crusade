<form class="{{cssClass}}" autocomplete="off">
  <header class="sheet-header">
    <img class="item-img" src="{{item.img}}" data-edit="img" title="{{item.name}}"/>
    <div class="header-fields">
      <h1 class="charname"><input name="name" type="text" value="{{item.name}}" placeholder="Name"/></h1>
    </div>
  </header>
<div>
    <label class='resource-label'>{{ localize (lookup @root.config.mySheetLabels "armo") }}</label>
    <input type='text' name='system.armor' value='{{system.armor}}' data-dtype='Number' />
</div>

{{!-- <div class="sheet-body">
  <div class="tab">
    <label class="resource-label">{{ localize (lookup @root.config.mySheetLabels "desc") }}</label>
    {{ editor enrichedDescription target="system.description" engine="prosemirror" button=true editable=editable }}
  </div>
  <div class="tab">
    <label class="resource-label">{{ localize (lookup @root.config.mySheetLabels "rule") }}</label>
    {{ editor system.rules target="system.rules" engine="prosemirror" button=true editable=editable }}
  </div>
</div> --}}

<table class="sheet-table">
  <thead>
    <tr>
      <th>{{ localize (lookup @root.config.mySheetLabels "desc") }}</th>
    </tr>
  </thead>
  <tbody class="tbody">
    <tr class="tr">
      <td class="td">{{ editor enrichedDescription target="system.description" engine="prosemirror" button=true editable=editable }}</td>
    </tr>
  </tbody>
</table>

<table class="sheet-table">
  <thead>
    <tr>
      <th>{{ localize (lookup @root.config.mySheetLabels "rule") }}</th>
    </tr>
  </thead>
  <tbody class="tbody">
    <tr class="tr">
      <td class="td">      {{editor system.rules target="system.rules" engine="prosemirror" button=true editable=editable}}</td>
    </tr>
  </tbody>
</table>




</form>
